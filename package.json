{
  "name": "darren-react-app",
  "version": "0.1.0",
  "description": "React web application built by Darren",
  "main": "src/index.tsx",
  "author": "Darren",
  "private": true,
  "dependencies": {
    "@material-ui/core": "^4.12.3",
    "@testing-library/jest-dom": "^5.11.4",
    "@testing-library/react": "^11.1.0",
    "@types/jest": "^26.0.15",
    "@types/material-ui": "^0.21.12",
    "@types/node": "^12.0.0",
    "@types/react": "^17.0.0",
    "@types/react-dom": "^17.0.0",
    "@types/react-router-dom": "^5.3.2",
    "babel-eslint": "^10.1.0",
    "eslint": "^7.32.0",
    "eslint-plugin-react": "^7.30.0",
    "parcel-bundler": "^1.12.5",
    "prettier": "^2.7.0",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-router-dom": "^6.0.2",
    "react-scripts": "4.0.3",
    "typescript": "^4.5.4",
    "typewriter-effect": "^2.18.2",
    "web-vitals": "^2.1.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "parcel-start": "parcel ./public/index.html --open",
    "parcel-build": "parcel build ./public/index.html --experimental-scope-hoisting",
    "lint": "eslint --ext .js,.ts,.jsx,.tsx src",
    "lint:fix": "eslint --ext .js,.ts,.jsx,.tsx src --fix",
    "format": "prettier --check \"src/**/*.{js,ts,jsx,tsx,scss}\"",
    "format:fix": "prettier --write \"src/**/*.{js,ts,jsx,tsx,scss}\"",
    "_comment": [
      "I use Parcel to compile my code before sending it to the browser, instead of using WebPack because Parcel requires less configuation",
      "I add the 'parcel-start' script to start the app with parcel, which automatically installs",
      "the associated dependencies and builds the app.",
      "For 'parcel-build' I add the --experimental-scope-hoisting flag to tree-shake (ask the software to inspect our",
      "use of the code and only include the functions that we're actually using in production — a process known as 'Tree Shaking'.)"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not ie <= 11",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version",
      "not ie <= 11"
    ]
  }
}
